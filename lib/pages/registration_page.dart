import 'package:flutter/material.dart';

// Theme
import 'package:todo/theme.dart';

class RegistrationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Register', style: blackTextStyle),
      ),
    );
  }
}
